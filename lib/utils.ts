// shadcn's newer styles use the `cn` package directly. AI Elements imports cn from
// "@/lib/utils" (the path in components.json), so this file points it at the same function.
export { cn } from "cn";
